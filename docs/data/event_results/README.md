# Event Results Mirror (GitHub Pages)

The canonical files live in `data/event_results/<EVENTID>.json`. Admin saves mirror the same payload to this folder so GitHub Pages can serve the JSON files without moving the SSOT.
